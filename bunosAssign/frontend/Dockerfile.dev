# 前端开发环境 Dockerfile
FROM node:18-alpine

# 安装git用于依赖安装
RUN apk add --no-cache git

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm ci

# 复制源代码
COPY . .

# 设置权限
RUN chown -R node:node /app
USER node

# 暴露端口
EXPOSE 8080

# 开发模式启动 (Vite热重载)
CMD ["npm", "run", "dev"]